{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "db:migrate",
      "type": "process",
      "command": "${config:python.defaultInterpreterPath}",
      "args": [
        "${workspaceFolder}/src/manage.py",
        "migrate"
      ]
    }
  ]
}